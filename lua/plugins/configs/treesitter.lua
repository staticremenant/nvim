local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "go", "proto" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
